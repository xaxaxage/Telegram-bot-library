Telegram bot library v3


Added bot ability to set command // Can be used to make commands menu (commands menu not available now, COMING SOON)

Example:

const Bot = new Bot(<token>)
Bot.setCommand("command", () => {
   FUNCTION BODY 
})